# frozen_string_literal: true
require 'rails_helper'

RSpec.describe Pawn, type: :model do
  # context 'valid_move?' do
  #   it 'should be valid pawn moves for first move' do

  #     # user = FactoryGirl.create(:user)
  #     # sign_in user
  #     pawn = Piece.create(type: 'King', x_coord: 2, y_coord: 2, first_move: true, user_id: user.user_id)
  #     expect(pawn.valid_move?(2, 3)).to be true
  #     expect(pawn.valid_move?(2, 4)).to be true
  #   end

  # end
end
